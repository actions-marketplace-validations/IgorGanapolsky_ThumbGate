name: Publish to NPM

on:
  push:
    branches: [main]
    paths:
      - 'package.json'
      - 'package-lock.json'
      - 'server.json'
      - '.github/workflows/publish-npm.yml'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      token_fallback:
        description: 'Emergency NODE_AUTH_TOKEN fallback. GAT bypass-2FA direct publish dies Jan 2027 — prefer OIDC.'
        type: boolean
        default: false

permissions:
  contents: write
  id-token: write

concurrency:
  group: publish-npm-${{ github.workflow }}-${{ github.ref }}
  # Never cancel a release after it may have created its immutable tag. A later
  # main push waits instead of stranding or racing the npm publication.
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          # npm provenance on GitHub Actions is most reliable on current LTS.
          node-version: '24.x'
          registry-url: 'https://registry.npmjs.org'
          # Trusted publishing: never cache the package manager in release builds
          # (docs.npmjs.com/trusted-publishers, 2026-09-04).
          package-manager-cache: false
      - name: Require npm CLI for OIDC trusted publishing
        run: |
          set -euo pipefail
          # Trusted publishing needs npm >= 11.5.1. Do not jump to npm 12 here:
          # v12 flipped install-script defaults (GitHub changelog 2026-07-08).
          # Parse patch: npm 11.5.0 is below the OIDC floor (see scripts/npm-oidc-cli-floor.js).
          current="$(npm -v)"
          echo "npm ${current}"
          node scripts/npm-oidc-cli-floor.js --version="$current"
      - run: npm ci --onnxruntime-node-install-cuda=skip
      - name: Run release safety checks
        run: |
          node scripts/sync-version.js --check
          npm run test:deployment
          npm run test:postinstall
          npm run prove:runtime
      - name: Audit npm package boundary
        run: npm pack --dry-run
      - name: Resolve package version
        id: package
        run: |
          version=$(node -e 'process.stdout.write(require("./package.json").version)')
          echo "version=$version" >> "$GITHUB_OUTPUT"
      - name: Check whether version is already on npm
        id: npm
        run: |
          if npm view "thumbgate@${{ steps.package.outputs.version }}" version >/dev/null 2>&1; then
            echo "published=true" >> "$GITHUB_OUTPUT"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Inspect release tag provenance
        if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
        id: tag
        env:
          VERSION: ${{ steps.package.outputs.version }}
        run: |
          remote_tag_sha="$(git ls-remote --tags origin "refs/tags/v${VERSION}" | awk '{print $1}')"

          if [ -n "$remote_tag_sha" ]; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "sha=$remote_tag_sha" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Plan publish action
        if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
        id: plan
        env:
          VERSION: ${{ steps.package.outputs.version }}
          CURRENT_SHA: ${{ github.sha }}
          CURRENT_BRANCH: ${{ github.ref_name }}
          DEFAULT_BRANCH: main
          NPM_PUBLISHED: ${{ steps.npm.outputs.published }}
          TAG_EXISTS: ${{ steps.tag.outputs.exists }}
          TAG_SHA: ${{ steps.tag.outputs.sha }}
        run: node scripts/publish-decision.js
      - name: Report publish no-op
        if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && steps.plan.outputs.skip_publish == 'true'
        run: echo "${{ steps.plan.outputs.reason }}"
      - name: Guard against silent no-op when main has content changes since last publish
        # Prevents the "1.5.2 already on npm, content changes don't ship" failure mode.
        # If the published version already exists but main has meaningful source changes
        # (README, HTML, scripts) since that tag, we should have bumped the version
        # unless an audited changeset is still pending.
        if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && steps.npm.outputs.published == 'true' && steps.plan.outputs.skip_publish == 'true'
        env:
          VERSION: ${{ steps.package.outputs.version }}
        run: |
          set -e
          # Find the last release tag. If main has commits touching shipped content
          # that aren't in that tag's tree, the author forgot to bump.
          LAST_TAG=$(git describe --tags --abbrev=0 --match='v*' 2>/dev/null || echo "")
          if [ -z "$LAST_TAG" ]; then
            echo "::notice::No prior release tag — cannot compare content drift."
            exit 0
          fi
          # Count changes in shipped surfaces since that tag. Include package
          # manifests because package boundary changes alter the npm tarball
          # even when runtime source files are unchanged.
          SHIPPED_CHANGES=$(git diff --name-only "$LAST_TAG"..HEAD -- \
            'package.json' 'package-lock.json' 'server.json' \
            'adapters/**' 'plugins/**' 'public/*.html' 'scripts/*.js' \
            'src/**' 'bin/**' 'README.md' 'CHANGELOG.md' 2>/dev/null | wc -l | tr -d ' ')
          PENDING_CHANGESETS=$(git diff --name-only "$LAST_TAG"..HEAD -- '.changeset/*.md' 2>/dev/null | grep -v '^.changeset/README.md$' | wc -l | tr -d ' ')

          # A pending changeset excuses the no-op only while a release is plausibly imminent.
          # It used to excuse it forever: on 2026-07-09 a merged security fix (#2807) sat
          # unshipped while this step printed a green notice, because a changeset existed.
          # An exemption with no expiry is a silence, not an audit. scripts/release-window.js
          # bounds it; see tests/release-window.test.js for the regression case.
          if [ "$SHIPPED_CHANGES" -gt 0 ] && [ "$PENDING_CHANGESETS" -gt 0 ]; then
            git diff --name-only "$LAST_TAG"..HEAD -- '.changeset/*.md' | grep -v '^.changeset/README.md$' | head -20
          fi
          SHIPPED_CHANGES="$SHIPPED_CHANGES" \
          PENDING_CHANGESETS="$PENDING_CHANGESETS" \
          LAST_TAG_TIMESTAMP="$(git log -1 --format=%ct "$LAST_TAG")" \
          NOW_TIMESTAMP="$(date -u +%s)" \
          MAX_UNRELEASED_DAYS="${MAX_UNRELEASED_DAYS:-7}" \
            node scripts/release-window.js || {
              echo "::error::Merged changes are not on npm and the release is overdue. Cut a release PR (bump package.json + consume changesets). Do not hand-publish."
              git diff --name-only "$LAST_TAG"..HEAD -- \
                'package.json' 'package-lock.json' 'server.json' \
                'adapters/**' 'plugins/**' 'public/*.html' 'scripts/*.js' \
                'src/**' 'bin/**' 'README.md' 'CHANGELOG.md' | head -20
              exit 1
            }
      - name: Create release tag
        if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && steps.plan.outputs.create_tag == 'true'
        env:
          VERSION: ${{ steps.package.outputs.version }}
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git tag "v${VERSION}"
          git push origin "v${VERSION}"
      - name: Publish npm package
        if: ((github.event_name == 'push' || github.event_name == 'workflow_dispatch') && steps.plan.outputs.publish_npm == 'true') || (github.event_name == 'release' && steps.npm.outputs.published == 'false')
        # OIDC-first (id-token: write above). A stale classic NPM_TOKEN returns
        # E404 "Not found" and masks trusted publishing. Only inject NODE_AUTH_TOKEN
        # when workflow_dispatch token_fallback=true or vars.THUMBGATE_NPM_TOKEN_FALLBACK=1.
        # Default is OIDC-only. GAT bypass-2FA direct publish is scheduled to die Jan 2027
        # (github.blog/changelog/2026-07-31-restricting-npm-bypass-2fa-granular-access-tokens).
        # Configs created after 2026-09-03 default to npm stage publish unless
        # "Allow npm publish" is checked on the Trusted Publisher form.
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_TOKEN_FALLBACK: ${{ github.event.inputs.token_fallback == 'true' && '1' || vars.THUMBGATE_NPM_TOKEN_FALLBACK }}
          NPM_TAG: ${{ steps.plan.outputs.npm_tag || 'latest' }}
        run: |
          set -euo pipefail
          if [ "${NPM_TOKEN_FALLBACK:-}" = "1" ] && [ -n "${NPM_TOKEN:-}" ]; then
            export NODE_AUTH_TOKEN="${NPM_TOKEN}"
            echo "Using NPM_TOKEN fallback (THUMBGATE_NPM_TOKEN_FALLBACK=1)."
          else
            unset NODE_AUTH_TOKEN || true
            echo "Publishing via GitHub Actions OIDC trusted publisher (no NODE_AUTH_TOKEN)."
          fi
          if ! npm publish --tag "${NPM_TAG}" --provenance; then
            echo "::error::npm publish failed. Configure Trusted Publisher on npmjs.com for package thumbgate → GitHub Actions → IgorGanapolsky/ThumbGate / publish-npm.yml (WebAuthn once), then re-run. Do not pin unpublished versions in customer emails (see thumbgate-npm-pin-honesty)."
            exit 1
          fi
      - name: Verify published npm runtime smoke
        if: ((github.event_name == 'push' || github.event_name == 'workflow_dispatch') && steps.plan.outputs.publish_npm == 'true') || (github.event_name == 'release' && steps.npm.outputs.published == 'false')
        env:
          VERSION: ${{ steps.package.outputs.version }}
        run: node scripts/prove-packaged-runtime.js --package-spec "thumbgate@${VERSION}" --expected-version "${VERSION}" --install-attempts 12 --install-delay-ms 10000
      - name: Verify published npm commit identity
        if: ((github.event_name == 'push' || github.event_name == 'workflow_dispatch') && steps.plan.outputs.publish_npm == 'true') || (github.event_name == 'release' && steps.npm.outputs.published == 'false')
        env:
          VERSION: ${{ steps.package.outputs.version }}
        run: |
          node scripts/verify-npm-githead.js \
            --version="${VERSION}" \
            --expected-sha="${GITHUB_SHA}" \
            --max-attempts=12 \
            --retry-delay-ms=10000 \
            --json
      - name: Resolve npm publish receipt
        id: receipt
        if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && steps.plan.outputs.ensure_release == 'true'
        env:
          VERSION: ${{ steps.package.outputs.version }}
        run: |
          node <<'NODE'
          const fs = require('node:fs');
          const { execFileSync } = require('node:child_process');

          const version = process.env.VERSION;
          const metadata = JSON.parse(execFileSync('npm', [
            'view',
            `thumbgate@${version}`,
            'dist',
            'time',
            '--json',
          ], { encoding: 'utf8' }));
          const dist = metadata.dist || {};
          const time = metadata.time || {};
          const lines = [
            `shasum=${dist.shasum || ''}`,
            `tarball=${dist.tarball || ''}`,
            `published_at=${time[version] || ''}`,
          ];

          fs.appendFileSync(process.env.GITHUB_OUTPUT, `${lines.join('\n')}\n`);
          NODE
      - name: Build full changeset release notes
        if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && steps.plan.outputs.ensure_release == 'true'
        env:
          VERSION: ${{ steps.package.outputs.version }}
          GITHUB_RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          NPM_SHASUM: ${{ steps.receipt.outputs.shasum }}
          NPM_TARBALL_URL: ${{ steps.receipt.outputs.tarball }}
          NPM_PUBLISHED_AT: ${{ steps.receipt.outputs.published_at }}
        run: |
          notes_file="thumbgate-${VERSION}-release-notes.md"
          node scripts/release-notes.js \
            --version="${VERSION}" \
            --current-ref="${GITHUB_SHA}" \
            --github-run-url="${GITHUB_RUN_URL}" \
            --npm-shasum="${NPM_SHASUM}" \
            --npm-tarball-url="${NPM_TARBALL_URL}" \
            --npm-published-at="${NPM_PUBLISHED_AT}" \
            --output="${notes_file}"
          cat "${notes_file}" >> "$GITHUB_STEP_SUMMARY"
      - name: Upload full release notes artifact
        if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && steps.plan.outputs.ensure_release == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          retention-days: 7
          name: thumbgate-${{ steps.package.outputs.version }}-release-notes
          path: thumbgate-${{ steps.package.outputs.version }}-release-notes.md
          if-no-files-found: error
      - name: Ensure GitHub Release exists
        if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && steps.plan.outputs.ensure_release == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.package.outputs.version }}
        run: |
          notes_file="thumbgate-${VERSION}-release-notes.md"
          if ! gh release view "v${VERSION}" >/dev/null 2>&1; then
            gh release create "v${VERSION}" --title "thumbgate@${VERSION}" --notes-file "${notes_file}"
          else
            gh release edit "v${VERSION}" --title "thumbgate@${VERSION}" --notes-file "${notes_file}"
          fi
          gh release upload "v${VERSION}" "${notes_file}" --clobber
