name: CI

on:
  push:
    # Pull requests already run this full suite. Running it again on every
    # feature-branch push doubles PR latency and spend without adding signal.
    branches: [main]
  pull_request:
    branches: [main]
  merge_group:
    branches: [main]
    types: [checks_requested]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  test:
    runs-on: ubuntu-latest
    # Keep enough headroom for the full suite plus proof lanes on main.
    timeout-minutes: 60
    env:
      STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
      STRIPE_PRICE_ID: ${{ secrets.STRIPE_PRICE_ID }}
      STRIPE_PRODUCT_ID: ${{ secrets.STRIPE_PRODUCT_ID }}
      STRIPE_PUBLISHABLE_KEY: ${{ secrets.STRIPE_PUBLISHABLE_KEY }}
      STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
      THUMBGATE_API_KEY: ${{ secrets.THUMBGATE_API_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0

      - name: Detect CI scope
        id: ci-scope
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || '' }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail

          # Modes:
          #   full — full suite + proofs (main, merge_group, runtime code PRs)
          #   web  — marketing/public/dashboard-only PRs
          #   deps — lockfiles, workflow pins, changesets only (biggest Dependabot win)
          # Merge queue and main always run full CI so required gates stay honest.

          if [[ "$EVENT_NAME" != "pull_request" ]]; then
            echo "mode=full" >> "$GITHUB_OUTPUT"
            echo "reason=non-pr-event" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Compare HEAD against the merge base, not the two branch tips.
          # Two-tip diff includes commits already on main that the PR lacks,
          # which wrongly forces full CI on stale Dependabot PRs.
          MERGE_BASE=$(git merge-base "$BASE_SHA" "$HEAD_SHA")
          echo "merge_base=$MERGE_BASE"
          git diff --name-only "${MERGE_BASE}...${HEAD_SHA}" | sed '/^$/d' > /tmp/ci-changed-files.txt
          echo "Changed files (vs merge base):"
          sed 's/^/- /' /tmp/ci-changed-files.txt || true

          if [[ ! -s /tmp/ci-changed-files.txt ]]; then
            echo "mode=full" >> "$GITHUB_OUTPUT"
            echo "reason=no-changed-files" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Pure dependency / CI-config surface (Dependabot action pins, lockfiles).
          # Does not include application source — those stay full.
          if ! grep -Ev '^(\.changeset/.*\.md|package\.json|package-lock\.json|workers/package\.json|workers/package-lock\.json|\.github/workflows/.*|\.github/dependabot\.yml)$' /tmp/ci-changed-files.txt > /tmp/ci-non-deps-files.txt; then
            echo "mode=deps" >> "$GITHUB_OUTPUT"
            echo "reason=deps-or-workflow-surface-only" >> "$GITHUB_OUTPUT"
            echo "Focused deps/workflow CI selected. Merge queue and main pushes still run full CI."
            exit 0
          fi

          if grep -Ev '^(\.changeset/.*\.md|public/|config/post-deploy-marketing-pages\.json|scripts/dashboard\.js|scripts/verify-marketing-pages-deployed\.js|tests/(api-server|dashboard|public-landing|landing-page-claims|funnel-invariants|verify-marketing-pages-deployed|public-static-assets)\.test\.js|tests/e2e/)' /tmp/ci-changed-files.txt > /tmp/ci-full-required-files.txt; then
            echo "mode=full" >> "$GITHUB_OUTPUT"
            echo "reason=runtime-or-workflow-surface" >> "$GITHUB_OUTPUT"
            echo "Files requiring full CI:"
            sed 's/^/- /' /tmp/ci-full-required-files.txt
            exit 0
          fi

          echo "mode=web" >> "$GITHUB_OUTPUT"
          echo "reason=web-revenue-surface-only" >> "$GITHUB_OUTPUT"
          echo "Focused web/revenue CI selected. Merge queue and main pushes still run full CI."

      - name: Setup Python
        if: steps.ci-scope.outputs.mode == 'full'
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Install Python deps
        if: steps.ci-scope.outputs.mode == 'full'
        # scikit-learn + joblib are required by scripts/eval_gate_classifier.py
        # so tests/eval-gate-classifier.test.js can exercise the actual ML
        # pipeline (train_and_score, joblib.dump, ROC-AUC, classification_report)
        # in CI instead of skipping the e2e branch. The package is opt-in for
        # end users — these installs only affect the CI runner.
        run: pip install pytest scikit-learn joblib
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: |
            package-lock.json
            workers/package-lock.json

      - name: Install deps
        run: npm ci --onnxruntime-node-install-cuda=skip

      - name: Audit root npm dependencies
        # Retry only transient registry/network failures. Deterministic audit
        # findings (vulns found) exit immediately. No sleep after the final attempt.
        run: |
          set +e
          for attempt in 1 2 3 4 5; do
            out=$(npm audit --audit-level=low 2>&1)
            code=$?
            printf '%s\n' "$out"
            if [ "$code" -eq 0 ]; then exit 0; fi
            transient=0
            printf '%s' "$out" | grep -Eqi '503 Service Unavailable|ECONNRESET|ETIMEDOUT|ENOTFOUND|network|socket hang up|audit endpoint returned an error|Service Unavailable' && transient=1
            if [ "$transient" -ne 1 ]; then
              echo "npm audit failed with non-transient exit ${code}; not retrying"
              exit "$code"
            fi
            if [ "$attempt" -ge 5 ]; then
              echo "npm audit exhausted retries after transient failures"
              exit "$code"
            fi
            echo "npm audit attempt ${attempt} transient failure (exit ${code}); retrying in $((attempt * 15))s"
            sleep $((attempt * 15))
          done
          exit 1

      - name: Audit worker npm dependencies
        run: |
          set -e
          npm --prefix workers ci
          set +e
          for attempt in 1 2 3 4 5; do
            out=$(npm --prefix workers audit --audit-level=low 2>&1)
            code=$?
            printf '%s\n' "$out"
            if [ "$code" -eq 0 ]; then exit 0; fi
            transient=0
            printf '%s' "$out" | grep -Eqi '503 Service Unavailable|ECONNRESET|ETIMEDOUT|ENOTFOUND|network|socket hang up|audit endpoint returned an error|Service Unavailable' && transient=1
            if [ "$transient" -ne 1 ]; then
              echo "workers npm audit failed with non-transient exit ${code}; not retrying"
              exit "$code"
            fi
            if [ "$attempt" -ge 5 ]; then
              echo "workers npm audit exhausted retries after transient failures"
              exit "$code"
            fi
            echo "workers npm audit attempt ${attempt} transient failure (exit ${code}); retrying in $((attempt * 15))s"
            sleep $((attempt * 15))
          done
          exit 1

      - name: Check version sync
        run: node scripts/sync-version.js --check

      - name: Check changeset coverage for release-relevant PRs
        # Dependabot PRs don't write changesets; auto-merge is the documented
        # path (.github/workflows/dependabot-automerge.yml). Skip changeset:check
        # for those so they're not stuck BLOCKED on `test`. 2026-06-08: surfaced
        # by #2582-#2585 all failing this exact step.
        if: (github.event_name == 'pull_request' || github.event_name == 'merge_group') && github.actor != 'dependabot[bot]' && github.event.pull_request.user.login != 'dependabot[bot]'
        run: npm run changeset:check

      - name: Run budget status gate
        run: npm run budget:status
      - name: Check operational integrity
        if: steps.ci-scope.outputs.mode == 'full'
        env:
          GH_TOKEN: ${{ github.token }}
        run: npm run ops:integrity:ci
      - name: Check branch protection congruence
        # Drift monitor — non-blocking. The script reads legacy Branch
        # Protection Rules via gh GraphQL/REST; if main has migrated to
        # Rulesets (Trunk-managed orgs do this), the script can't see
        # the rule and exits 1. We don't want a periodic monitor to
        # block PRs — surface drift in the run UI without failing the
        # job. Companion step below checks repository rulesets.
        if: github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]'
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GH_PAT || github.token }}
        run: npm run branch-protection:check
      - name: Check repository ruleset congruence
        # Non-blocking drift monitor for the active main governance ruleset
        # (zero bypass_actors + merge-quality required checks + dual classic/ruleset compare).
        if: github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]'
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GH_PAT || github.token }}
        run: npm run rulesets:check

      - name: Run focused web/revenue tests
        if: steps.ci-scope.outputs.mode == 'web'
        run: |
          node --test \
            tests/api-server.test.js \
            tests/dashboard.test.js \
            tests/public-landing.test.js \
            tests/funnel-invariants.test.js \
            tests/landing-page-claims.test.js \
            tests/verify-marketing-pages-deployed.test.js \
            tests/public-static-assets.test.js

      - name: Run focused deps/workflow smoke tests
        if: steps.ci-scope.outputs.mode == 'deps'
        # Dependabot / workflow-pin PRs were waiting ~14m on the full suite.
        # Keep a tight smoke that still exercises packaging + CI contracts.
        # Full suite still runs on merge_group and main.
        env:
          THUMBGATE_PUBLISH_STATE: unpublished
          THUMBGATE_PUBLISHED_CLI_STATE: unavailable
        run: |
          set -euo pipefail
          node --test \
            tests/public-core-boundary.test.js \
            tests/public-bundle-ratchet.test.js \
            tests/package-boundary.test.js \
            tests/deployment.test.js \
            tests/sonarcloud-workflow.test.js \
            tests/ci-cd-hygiene-audit.test.js
          MERGE_BASE=$(git merge-base "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}")
          if git diff --name-only "${MERGE_BASE}...${{ github.event.pull_request.head.sha }}" | grep -qE '^workers/'; then
            npm run test:workers
          fi
          if git diff --name-only "${MERGE_BASE}...${{ github.event.pull_request.head.sha }}" | grep -qE '^(package\.json|package-lock\.json)$'; then
            npm run test:public-package-parity
          fi

      - name: Run tests
        if: steps.ci-scope.outputs.mode == 'full'
        # Hermetic suite: short-circuit the npm-registry publish probe in
        # scripts/mcp-config.js (isVersionPublished -> `npm view thumbgate@<v`),
        # which several CLI/statusline tests exercise indirectly. Without this,
        # the suite depends on npm-registry reachability and intermittently
        # stalls in CI. The probe and its 5s timeout stay intact for real users.
        #
        # NODE_V8_COVERAGE collects raw V8 coverage for every spawned node
        # process during the suite. The "Generate LCOV coverage for SonarCloud"
        # step below converts it, and SonarCloud downloads that artifact instead
        # of re-running the entire suite (the old SonarCloud job duplicated this
        # ~10 min run on every PR).
        env:
          THUMBGATE_PUBLISH_STATE: unpublished
          THUMBGATE_PUBLISHED_CLI_STATE: unavailable
          NODE_V8_COVERAGE: .coverage/raw
        run: npm test

      - name: Generate LCOV coverage for SonarCloud
        if: steps.ci-scope.outputs.mode == 'full'
        run: |
          npx c8 report \
            --temp-directory .coverage/raw \
            --reports-dir coverage \
            --reporter=lcov \
            --reporter=text-summary \
            --include='.claude/**/*.js' \
            --include='adapters/**/*.js' \
            --include='bin/**/*.js' \
            --include='plugins/**/*.js' \
            --include='scripts/**/*.js' \
            --include='src/**/*.js' \
            --exclude='tests/**/*.js' \
            --exclude='scripts/social-reply-monitor.js'

      - name: Upload LCOV coverage for SonarCloud
        if: steps.ci-scope.outputs.mode == 'full'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: coverage-lcov
          path: coverage/lcov.info
          retention-days: 1
          if-no-files-found: error

      - name: Run gate-eval regression suite
        if: steps.ci-scope.outputs.mode == 'full'
        # Exits non-zero if any case in config/evals/*.json fails against
        # config/specs/*.json. Any constraint regex change that flips a
        # previously-blocking case to pass (or vice versa) blocks merge
        # until the eval case is consciously updated in the same PR.
        run: npm run gate-eval:ci

      - name: Verify double-blind holdout seal
        if: steps.ci-scope.outputs.mode == 'full'
        # Fail-closed: the holdout manifest's seal (partition reproducibility,
        # no case swap-in, no raw question leakage) must be intact before any
        # release. Non-zero exit = broken seal = block merge.
        run: npm run eval:holdout:check

      - name: Verify holdout verdicts (regression gate)
        if: steps.ci-scope.outputs.mode == 'full'
        # Non-zero exit = holdout verdicts drifted = block merge until re-freeze.
        run: npm run eval:holdout:verify

      - name: Run coverage
        # Skip on ordinary PRs: coverage re-runs the full suite and doubles wall clock.
        # Main + merge_group still re-run coverage so the merge gate keeps the signal.
        if: steps.ci-scope.outputs.mode == 'full' && github.event_name != 'pull_request'
        # Same hermetic override as "Run tests" — coverage re-runs the suite.
        env:
          THUMBGATE_PUBLISH_STATE: unpublished
          THUMBGATE_PUBLISHED_CLI_STATE: unavailable
        run: npm run test:coverage

      - name: Check congruence (branding, tech stack, version across all surfaces)
        if: steps.ci-scope.outputs.mode == 'full'
        run: npm run test:congruence

      - name: Run adapter proof
        if: steps.ci-scope.outputs.mode == 'full'
        run: npm run prove:adapters

      - name: Run automation proof
        if: steps.ci-scope.outputs.mode == 'full'
        run: npm run prove:automation

      - name: Run runtime proof
        if: steps.ci-scope.outputs.mode == 'full'
        run: npm run prove:runtime

      - name: Run evolution proof
        if: steps.ci-scope.outputs.mode == 'full'
        run: npm run prove:evolution

      - name: Run workflow contract proof
        if: steps.ci-scope.outputs.mode == 'full'
        run: npm run prove:workflow-contract

      - name: Run autoresearch proof
        if: steps.ci-scope.outputs.mode == 'full'
        run: npm run prove:autoresearch

      - name: Run Tessl distribution proof
        if: steps.ci-scope.outputs.mode == 'full'
        run: npm run prove:tessl

      - name: Run evaluation regression gates
        if: always() && steps.ci-scope.outputs.mode == 'full'
        run: |
          mkdir -p proof
          node scripts/prompt-eval.js --min-score=100 --synthetic --synthetic-variants=1 --baseline=config/evals/prompt-eval-baseline.json --require-no-regressions --suite-output proof/prompt-eval-suite.generated.json --output proof/prompt-eval-report.json --json > /dev/null
          node scripts/agent-outcome-eval.js --baseline=config/evals/agent-outcomes-baseline.json --output=proof/agent-outcome-eval-report.json > /dev/null
          node -e "const fs=require('node:fs'); const report=JSON.parse(fs.readFileSync('proof/prompt-eval-report.json','utf8')); const lines=['## Prompt Evaluation', '- Score: ' + report.score + '%', '- Passed: ' + report.passed + '/' + report.total, '- Failed: ' + report.failed, '- Errors: ' + report.errors, '- Skipped: ' + report.skipped, '- Synthetic cases: ' + (report.syntheticCount || 0)]; if (report.comparison) lines.push('- Regressions: ' + report.comparison.regressions.length); fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY, lines.join('\n') + '\n');"
          node -e "const fs=require('node:fs'); const report=JSON.parse(fs.readFileSync('proof/agent-outcome-eval-report.json','utf8')); const lines=['## Verified Agent Outcomes', '- Score: ' + report.score + '%', '- Passed: ' + report.passed + '/' + report.total, '- Regressions: ' + report.regressions.length, '- Evidence: ' + report.evidenceStatus]; fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY, lines.join('\n') + '\n');"

      - name: Sync GitHub About metadata on main
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
          THUMBGATE_GITHUB_ABOUT_VERIFY_ATTEMPTS: 6
          THUMBGATE_GITHUB_ABOUT_VERIFY_DELAY_MS: 5000
        run: npm run github:about:sync

      - name: Verify live GitHub About congruence on main
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          GITHUB_TOKEN: ${{ github.token }}
          THUMBGATE_GITHUB_ABOUT_VERIFY_ATTEMPTS: 6
          THUMBGATE_GITHUB_ABOUT_VERIFY_DELAY_MS: 5000
        run: npm run test:congruence:live

      - name: Upload proof artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          retention-days: 7
          name: proof-artifacts
          path: |
            proof/compatibility/report.json
            proof/compatibility/report.md
            proof/automation/report.json
            proof/automation/report.md
            proof/runtime-report.json
            proof/runtime-report.md
            proof/evolution-report.json
            proof/evolution-report.md
            proof/workflow-contract/report.json
            proof/workflow-contract/report.md
            proof/autoresearch-report.json
            proof/autoresearch-report.md
            proof/tessl-report.json
            proof/tessl-report.md
            proof/prompt-eval-report.json
            proof/prompt-eval-suite.generated.json
            proof/agent-outcome-eval-report.json
